import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModificarmacetaPage } from './modificarmaceta.page';

describe('ModificarmacetaPage', () => {
  let component: ModificarmacetaPage;
  let fixture: ComponentFixture<ModificarmacetaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ModificarmacetaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
