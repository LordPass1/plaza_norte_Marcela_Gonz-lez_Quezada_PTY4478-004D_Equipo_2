import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BanearusuarioPage } from './banearusuario.page';

describe('BanearusuarioPage', () => {
  let component: BanearusuarioPage;
  let fixture: ComponentFixture<BanearusuarioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(BanearusuarioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
